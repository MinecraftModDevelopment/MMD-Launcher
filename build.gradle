subprojects {
    apply plugin: "java"

    group = "com.mcmoddev"
    archivesBaseName = "launcher"
    version = "0.1.1-develop"
    sourceCompatibility = targetCompatibility = "1.8"

    repositories {
        mavenCentral()
    }
}
